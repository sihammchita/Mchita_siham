{% extends 'base.html.twig' %}

{% block title %}Realisteurs{% endblock %}

{% block body %}
    <h1>Realisteurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ realisteur.id }}</td>
            </tr>
            <tr>
                <th>Id_real</th>
                <td>{{ realisteur.idReal }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ realisteur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ realisteur.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_realisteurs_index') }}">back to list</a>

    <a href="{{ path('app_realisteurs_edit', {'id': realisteur.id}) }}">edit</a>

    {{ include('realisteurs/_delete_form.html.twig') }}
{% endblock %}
